services:
  - type: worker
    name: maps-scraper
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y chromium
      pip install -r requirements.txt
    startCommand: python maps_scraper.py